# 路径规划器配置（按类别组织）

visualization:
  # 同时用于栅格与路径可视化
  scale: 4
  show_grid_lines: true
  use_antialiasing: true
  image_format: "png"
  save_path_visualization: true
  output_dir: "/root/xline_ws/xline_path_planner/path_visualizations"   # 栅格/路径可视化输出目录
  planned_output_dir: "/root/xline_ws/xline_path_planner/planned_results"  # 规划JSON输出目录

cad_parser:
  unit_conversion_factor: 1000.0   # 毫米->米
  auto_scale_coordinates: true
  angle_unit: "DEGREES"            # 可选: DEGREES | RADIANS
  cad_file_dir: "/root/xline_ws/xline_path_planner/cad_data"   # CAD文件所在目录（服务端）

grid_map:
  resolution: 0.005   # 米/格
  padding: 0.1        # 地图边界填充(米)

offsets:
  left_offset: -0.05      # 左侧打印机偏移(m)
  right_offset: 0.05      # 右侧打印机偏移(m)
  center_offset: 0.05      # 中心打印机偏移(m)

path_planner:
  path_extension_length: 0.1

# 可选：TCP服务器配置（用于非ROS客户端调用）
tcp_server:
  enabled: true
  host: "192.168.43.85"
  port: 52088
